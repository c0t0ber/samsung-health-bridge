<resources>
    <style name="Theme.SamsungHealthBridge" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:colorAccent">@color/bridge_primary</item>
        <item name="android:navigationBarColor">@color/bridge_background</item>
        <item name="android:statusBarColor">@color/bridge_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/bridge_background</item>
    </style>
</resources>
